@model List<TourManagement.NhanVien>

@{
    ViewData["Title"] = "Nhân Viên";
}

<h2>Quản lý nhân viên</h2>

<p>
    @Html.ActionLink("Thêm nhân viên mới", "Create")
</p>


<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Tìm kiếm theo tên: <input type="text" name="SearchString" />
            <input type="submit" value="Tìm kiếm" class="btn btn-default" /> |
            @Html.ActionLink("Làm mới", "Index")
        </p>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                Mã nhân viên
            </th>
            <th>
                Họ tên
            </th>
            <th>
                Hành động
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HoTen)
                </td>
                <td>
                    @Html.ActionLink("Sửa", "Edit/" + @item.Id, @item.Id, item)  |
                    @Html.ActionLink("Xóa", "Delete/" + @item.Id, @item.Id, item) |
                    @Html.ActionLink("Phân công", "Index/" + @item.Id,"PhanCong", @item.Id, item)
                </td>
            </tr>
        }
    </tbody>
</table>